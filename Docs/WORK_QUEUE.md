# SIGNAL RUSH 작업 큐

이 문서는 사람 작업자가 다음 작업의 조건과 검증 방법을 빠르게 확인하기 위한 큐다. 기능 정의의 정본은 `SIGNAL_RUSH_GDD_v0.2.md`이며, 구현 세부 기준은 `PROJECT_GUIDE.md`를 따른다.

## 확정 결정

- 조작은 점프와 공격 두 버튼만 사용한다. 좌우 이동 입력은 구현하지 않는다.
- 수평 이동과 화면 내 위치는 자동 전진, 감가속, 리스폰, 카메라 연출이 통제한다.
- 현재 작업은 F2 점프 궤적과 F6 도달 가능한 청크 생성까지만 포함한다.
- F6 계산을 단순한 이차함수로 유지하기 위해 `TUNE-P2`, `TUNE-P3` 감쇠 중력은 구현하지 않는다.
- 공격 판정은 입력 순간 한 번만 수행한다. `attackDuration`은 캐릭터 연출 상태다. 재입력 제한의 정본은 아래 P0 결정 대기다.
- 장시간 공중 상태는 게임오버가 아니라 `_isRespawning` 상태로 전환한다.
- 피격, 체력, 콤보와 패링의 총알·피해 연결은 아래 대기 큐의 조건을 다시 검토한 뒤 구현한다.

## 현재 상태

| 우선순위 | 작업 | 상태 |
|---|---|---|
| P0 | Q1 F2 점프 궤적 | 자동 검증 통과 — 사람 플레이 대기 |
| P0 | Q2 F6 도달 가능한 청크 | 자동 검증 통과 — 60초 플레이 대기 |
| P1 | Q3 F1 자동 조작과 카메라 | 대기 |
| P1 | Q4 F3 피격과 체력 | 대기 |
| P2 | Q5 F5 콤보 | 대기 |
| P2 | Q6 F4 패링 | 부분 구현 — 타이밍·사운드 자동 검증 통과, 총알·피해 연결 대기 |

## 진행 중 — Q1: F2 점프 궤적 고정

**목표:** 같은 초기 조건에서 항상 같은 점프 궤적을 만들고 F6가 도달 가능 범위를 계산할 수 있게 한다.

**완료 조건:**

- [ ] 접지 중 점프 입력 1회만 수직 속도를 부여하며 공중 입력은 무시한다.
- [ ] 점프 높이는 버튼을 누른 시간에 의존하지 않는다. 기존 hold force 방식은 제거한다.
- [ ] 상승 중에는 정상 중력을 사용한다.
- [ ] 상승과 낙하 모두 일정한 중력을 사용해 궤적을 이차함수로 유지한다.
- [ ] `TUNE-P2`, `TUNE-P3` 필드와 감쇠 중력 코드를 만들지 않는다.
- [ ] 계산용 궤적은 실제 `Rigidbody2D.gravityScale`과 같은 중력을 사용한다.

**최소 인터페이스:**

```csharp
public Vector2 Player.CanReachChunk(Transform start, Transform end);
```

`CanReachChunk`는 콤보가 0인 기본 속도, 현재 중력과 안전 마진으로 수평·수직 여유를 반환한다. F6 외 소비자가 생기기 전에는 별도 궤적 타입이나 설정 클래스를 만들지 않는다.

**자동 검증:**

- `TraversalContractTests`: 보수적인 평지 간격에는 양수 여유를, 불가능한 간격에는 음수 여유를 반환하는지 검증한다.
- `SceneSmokeTests`: 고정 시드로 생성된 청크의 수평·수직 여유가 모두 음수가 아닌지 검증한다.

## 대기 — Q2: F6 도달 가능한 청크 생성

**선행 조건:** Q1의 이차 궤적 계산이 실제 Rigidbody2D 중력과 일치해야 한다.

**완료 조건:**

- [ ] 다음 청크의 `StartPoint`는 직전 청크 `EndPoint`에서 시작한다.
- [ ] 무작위 오프셋은 생성 전에 `Player.CanReachChunk`로 검증한다.
- [ ] 상승 높이는 최대 도달 높이에서 안전 마진을 뺀 값 이하로 제한한다.
- [ ] 수평 간격은 콤보 0의 기본 속도에서 착지 가능한 범위로 제한한다.
- [ ] 도달 불가능한 표본은 그대로 배치하지 않고 안전 경계로 제한한다.
- [ ] 장애물은 소속 청크의 자식으로 생성해 청크 폐기와 함께 제거한다.
- [ ] 고정 난수 시드로 생성한 연속 청크가 모두 도달 가능해야 한다.
- [ ] 사람 플레이 테스트에서 60초 동안 진행 불가 구간이 없어야 한다.

**자동 검증:**

- `SceneSmokeTests`: 고정 시드에서 연속 생성된 청크의 모든 오프셋을 `Player.CanReachChunk`로 검사한다.
- 기존 씬·프리팹 계약 테스트는 계속 통과해야 한다.

## 대기 — Q3: F1 자동 조작과 카메라

- 좌우 입력을 추가하지 않는다.
- 감가속과 리스폰 중에도 플레이어를 화면 안에 유지한다.
- 카메라는 전방 판단 공간을 확보하고 점프 중 과도한 세로 흔들림을 만들지 않는다.
- Q1/Q2가 안정된 뒤 감가속과 화면 위치 복귀 연출을 별도 설계한다.

## 완료 — 공격 판정·연출 분리와 리스폰 상태 계약

- 공격 히트박스는 입력 직후 한 물리 판정까지만 활성화한다.
- `attackDuration` 동안 캐릭터 공격 연출만 유지한다.
- 재입력 제한 시점은 확정하지 않는다. 아래 P0에서 `attackCooldown`과 최근 애니메이션 타이밍 중 정본을 결정한다.
- `Respawn()`은 `_isRespawning`을 켜고 `_respawnElapsed`를 초기화하며 게임 시간을 멈추지 않는다.
- 다음 청크의 시작점보다 `5f` 위에서 낙하시키고 `respawnTime` 뒤 `_isRespawning`을 해제한다.

## 대기 — Q4: F3 피격과 체력

- 공격 중 재입력은 무시한다.
- 몸통이 장애물과 충돌하면 체력 1 감소, 콤보 0, 무적 시간을 시작한다.
- 장애물은 피격으로 파괴되지 않는다.
- 무적 중 추가 피해만 무시하고 점프와 공격은 유지한다.
- 낙사 처리는 GDD Q3 결정 후 구현한다.

## 대기 — Q5: F5 콤보

- 장애물 파괴와 패링 성공마다 정확히 1 증가한다.
- 상한은 20이며 콤보 20에서 기본 속도의 120%가 된다.
- 피격 시 즉시 0이 되고 시간에 따라 감소하지 않는다.
- 최고 콤보와 파괴 수 기록은 결과 화면 작업에서 연결한다.

## 대기 — Q6: F4 패링

- 총알이 존재하는 동안 공격 입력 시 패링이 성립한다.
- 성공한 총알은 피해를 주지 않고 콤보를 1 올린다.
- 예고 단계나 총알이 없는 공격은 패링으로 처리하지 않는다.
- 스나이퍼·총알의 청크 소유권과 최대 활성 수를 함께 검증한다.

## 최근 패링·사운드 수선 결과 (2026-08-21)

- Player는 조준마다 독립된 실시간 패링 창을 유지하며, 나중 조준이 먼저 예약된 창을 취소하지 않는다.
- 패링은 창 안의 공격에서 한 번만 성립하고, 창 전·후 공격과 0초 창은 성공하지 않는다.
- Sniper는 예약된 발사를 새 난수 판정으로 덮어쓰지 않으며, 더블 경고를 같은 Heartbeat에 겹쳐 발행하지 않는다.
- Player와 Sniper는 선택적 런타임 의존성·오디오 참조가 없어도 예외를 내지 않고, 파괴 시 이벤트 구독을 해제한다.
- SampleScene의 Clock/Sniper 단일 인스턴스와 Player/Sniper 오디오 참조를 EditMode 계약으로 고정했다.
- 생성 장애물은 다시 소속 청크의 자식으로 생성된다.
- 자동 검증: EditMode 5/5, PlayMode 17/17, C# 빌드 오류 0개.
- GDD v0.2의 사운드 제외 결정은 변경하지 않았다. 현재 SFX는 최근 커밋의 부분 구현으로만 관리한다.

### 사람 작업 큐

- [ ] **P0 — F3 재입력 정책 결정:** GDD/기존 큐의 `attackCooldown`과 최근 애니메이션의 `attack12Interval`·`afterAttackInterval` 중 정본을 고른 뒤 해당 경계 테스트를 복구한다.
- [ ] **P1 — 실제 총알 연결:** 시간 창 대리 상태를 총알 존재·무효화와 연결하고, 성공 시 피해 0·콤보 +1을 검증한다.
- [ ] **P1 — 실패 처리 연결:** 패링 실패 시 체력 감소·콤보 0·무적 시간을 Q4와 함께 구현한다.
- [ ] **P1 — 스나이퍼 수명주기:** 청크 소유권, 예고 중 청크 보존, 동시 활성 최대 2기를 검증한다.
- [ ] **P2 — 시각 피드백:** 「!」·붉은 선·총알 반사·플래시를 연결해 사운드 없이도 판독 가능한지 확인한다.
- [ ] **P2 — 사람 청감 테스트:** 일반/더블 경고와 패링 성공 SFX의 실제 재생, FX 믹서 라우팅, 음량 밸런스를 에디터와 Windows 빌드에서 확인한다.

## 작업 순서

1. Q1 테스트를 실패 상태로 확인한다.
2. Q1을 최소 구현하고 전체 테스트를 통과시킨다.
3. Q2 생성 테스트를 실패 상태로 확인한다.
4. Q2를 최소 구현하고 60초 사람 플레이를 수행한다.
5. Q3 이후는 큐의 조건을 재검토한 뒤 하나씩 활성화한다.

## 폐기된 RED 기준선 (2026-08-21)

- EditMode: 전체 12개 중 기존 계약 5개 통과, 새 F2/F6 계약 7개 실패.
- PlayMode: 전체 4개 중 기존 스모크 1개 통과, 새 계약 3개 실패.
- 이 기준선은 `TUNE-P2`, `TUNE-P3` 폐기 전 계약이므로 더 이상 완료 판정에 사용하지 않는다.

## Q1/Q2 최초 구현 진단 기록 (2026-08-21)

- EditMode: 5개 중 2개 통과, 3개 실패.
- PlayMode 전체: 6개 중 1개 통과, 5개 실패. 빈 청크 배열의 첫 예외가 뒤 테스트까지 오염하므로 기능별 필터 결과를 함께 본다.
- Q1 상수 중력: 필터 테스트 통과. 다만 `CanReachChunk`가 씬의 `gravityScale`과 점프 시 한 번 더하는 힘을 계산에 포함하지 않아 실제 궤적과 아직 일치하지 않는다.
- Q2 생성: `PlatformManager.buildings`와 `obstacles`가 필드명 변경 중 빈 배열로 저장되어 시작 즉시 실패한다. 생성기는 별도 GameObject이므로 `GetComponent<Player>()`도 유효하지 않다.
- 공격: 판정과 연출 분리는 통과했지만, 재공격 가능 시점이 `attackDuration + attackCooldown`으로 계산되어 입력 기준 쿨타임 계약은 실패한다.
- 리스폰: `_isRespawning` 필드는 있지만 `Respawn()`이 상태를 켜지 않은 채 게임오버와 `timeScale = 0`을 실행해 실패한다.
- 컨벤션: `PlatBuilding` 직렬화 필드가 아직 `_camelCase`다. `Player.ApplyJumpHoldForce()`와 주석 처리된 호출부도 새 `[Obsolete]` 제거 규칙의 정리 대상이다.

## Q1/Q2 수정 후 검증 결과 (2026-08-21)

- EditMode: 5/5 통과.
- PlayMode: 8/8 통과.
- `CanReachChunk`는 실제 `Physics2D.gravity * Rigidbody2D.gravityScale`과 고정 점프 출력을 사용한다.
- 생성 청크 30개의 도달 여유와 생성 장애물의 청크 소유권을 고정 시드로 검증했다.
- 낙사 시 콤보를 초기화하고 플레이어 앞의 가장 가까운 청크 시작점보다 `5f` 위에서 낙하시킨다.
- 남은 완료 조건은 에디터에서 60초 사람 플레이로 진행 불가 구간과 리스폰 연출 감각을 확인하는 것이다.
